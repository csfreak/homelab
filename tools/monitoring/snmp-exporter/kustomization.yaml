resources:
  - imagestreams.yaml
  - deploy.yaml
  - service.yaml
  - probes.yaml
  - service-monitor.yaml

commonLabels:
  app: snmp-exporter

labels:
  - pairs:
      app.kubernetes.io/component: exporter
      app.kubernetes.io/part-of: snmp-exporter

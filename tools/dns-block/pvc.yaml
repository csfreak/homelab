apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-config-2
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: synology-iscsi-hdd
  volumeMode: Filesystem

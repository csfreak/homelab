apiVersion: v1
kind: Service
metadata:
  name: smtp
spec:
  ports:
    - name: smtp
      port: 25
      protocol: TCP
      targetPort: smtp
    - name: metrics
      protocol: TCP
      port: 9154
      targetPort: metrics
      appProtocol: http
  selector:
    deployment: smtp-relay
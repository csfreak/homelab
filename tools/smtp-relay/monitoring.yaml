---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: postfix-metrics
spec:
  selector:
    matchLabels:
      app: smtp-relay
  endpoints:
    - port: metrics
      path: /metrics
      interval: 10s

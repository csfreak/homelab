apiVersion: v1
kind: Service
metadata:
  name: plex-metrics
  namespace: media
  labels:
    k8s-app: plex-metrics
spec:
  ports:
    - name: metrics
      port: 80
      protocol: TCP
      targetPort: 9594
  selector:
    app: plex-metrics
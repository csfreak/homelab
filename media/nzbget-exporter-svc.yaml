apiVersion: v1
kind: Service
metadata:
  name: nzbget-metrics
  namespace: media
  labels:
    k8s-app: nzbget-metrics
spec:
  ports:
    - name: metrics
      port: 80
      protocol: TCP
      targetPort: 9452
  selector:
    app: nzbget-metrics
apiVersion: v1
data:
  ALLOWED_HOSTS: localhost netbox.homelab.csfreak.com
  DB_HOST: netbox-postgres
  REDIS_HOST: netbox-redis
  METRICS_ENABLED: 'true'
kind: ConfigMap
metadata:
  name: netbox-config
  namespace: netbox

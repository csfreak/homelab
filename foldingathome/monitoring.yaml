apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: fah-metrics
spec:
  namespaceSelector:
    matchNames:
      - foldingathome
  selector:
    matchLabels:
      app: fah-cpu
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
      interval: 10s

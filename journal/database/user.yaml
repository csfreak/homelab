apiVersion: mariadb.mmontes.io/v1alpha1
kind: User
metadata:
  name: monica
spec:
  mariaDbRef:
    name: journal-db
  passwordSecretKeyRef:
    name: monica-db-user
    key: PASSWORD
  maxUserConnections: 20

apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: plex-metrics
  namespace: monitoring
  labels:
    k8s-app: plex-metrics
spec:
  namespaceSelector:
    matchNames:
      - media
  selector:
    matchLabels:
      k8s-app: plex-metrics
  endpoints:
    - port: metrics
      path: /
      interval: 10s

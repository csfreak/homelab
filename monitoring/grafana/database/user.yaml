apiVersion: mariadb.mmontes.io/v1alpha1
kind: User
metadata:
  name: grafana-db
spec:
  mariaDbRef:
    name: grafana
  passwordSecretKeyRef:
    name: grafana-db-user
    key: PASSWORD
  maxUserConnections: 20

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-cert
  namespace: monitoring
  labels:
    app.kubernetes.io/instance: monitoring
spec:
  commonName: grafana.monitoring.apps.snolab.csfreak.com
  dnsNames:
    - grafana.monitoring.apps.snolab.csfreak.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: grafana-cert

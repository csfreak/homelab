apiVersion: rbac.authorizations.k8s.io/v1
kind: Role
metadata:
  name: mariadb-admin
  labels:
    rbac.authorization.k8s.io/aggregate-to-admin: true
rules:
  - apiGroups:
      - mariadb.mmontes.io
    resources:
      - "*"
    verbs:
      - "*"

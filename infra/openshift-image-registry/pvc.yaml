kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: image-registry-iscsi
  namespace: openshift-image-registry
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  volumeName: image-registry
  storageClassName: synology-iscsi
  volumeMode: Filesystem

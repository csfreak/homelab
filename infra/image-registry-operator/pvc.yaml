kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: image-registry-shared
  namespace: openshift-image-registry
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: synology-nfs-storage-ssd
  volumeMode: Filesystem

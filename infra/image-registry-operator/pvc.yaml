kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: image-registry-iscsi
  namespace: openshift-image-registry
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: synology-nfs-ssd
  volumeMode: Filesystem

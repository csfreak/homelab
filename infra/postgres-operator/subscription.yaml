apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: postgresql
  namespace: infra-postgres-operator
spec:
  channel: v5
  installPlanApproval: Automatic
  name: postgresql
  source: opertatorhubio
  sourceNamespace: openshift-marketplace
  startingCSV: postgresoperator.v5.5.2

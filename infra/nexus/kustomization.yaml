resources:
  - certificate.yaml
  - route.yaml
  - service.yaml
  - haproxy-deploy.yaml
  - metrics.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: nexus3
    includeTemplates: true

configMapGenerator:
  - name: haproxy-config
    files:
      - haproxy.cfg

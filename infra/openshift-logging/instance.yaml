apiVersion: logging.openshift.io/v1
kind: ClusterLogging
metadata:
  name: instance
spec:
  collection:
    type: vector
  logStore:
    lokistack:
      name: lokistack-logging
    type: lokistack
  managementState: Managed
  visualization:
    type: ocp-console

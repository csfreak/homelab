apiVersion: nmstate.io/v1
kind: NodeNetworkConfigurationPolicy
metadata:
  name: dns-search-domain
spec:
  desiredState:
    dns-resolver:
      config:
        search: []
      running:
        search: []
  maxUnavailable: 1
